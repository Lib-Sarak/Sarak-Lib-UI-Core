# Índice de Implementação das Especificações

**É o arquivo de entrada obrigatório**: abra este arquivo sempre que for executar qualquer spec de `specs/plan/`, antes de editar qualquer coisa.

## Princípio vigente do planejamento

> ✅ **A Sarak-Lib-UI-Core é uma BASE de front com Shell + Design Engine central, consumida pelo modelo de MÓDULOS-PLUGIN** (decisão do mantenedor, executada pela [Spec 43](./43-design-system-primeiro.md), 2026-07-23). O sistema importador **registra seus módulos de negócio** (React) na base — `registerSarakModule`/`registerLocalComponent` — sobre `SarakUIProvider`+`SarakShell`; a base fornece o **Shell** (navegação/layout), o **Design Engine** (central de tema/template que aplica a TODA tela que consome os tokens `var(--sarak-*)`) e os **componentes atômicos** como blocos. O importador altera o layout pela central e pode **criar o que precisar** (módulo/componente próprio) — desde que use os tokens públicos para responder à central (marcação hardcoded fora do contrato de tokens NÃO é tematizada). É exatamente o padrão do `Sarak-MyService`, o único consumidor real da lib, agora oficial. O renderizador de páginas por manifesto (`src/core/Manifest/`) **foi REMOVIDO na [Spec 46](./46-remover-motor-de-manifesto.md) (2026-07-25)**, depois do Teste Real (Spec 40) provar o modelo de módulos-plugin em produção real; **Shell/Discovery (#1) + Provider/Design Engine/componentes atômicos (#3) seguem** — são o produto.
>
> **(Histórico da virada, 2026-07-22 a 23)** O princípio anterior ("renderizador genérico, 100% via manifesto, zero React no consumidor") **falhou na prática**: o Teste Real bateu em 4 paredes numa tela simples; o motivo é estrutural (renderizador declarativo genérico é reconstruir uma linguagem em JSON — o gap nunca fecha). A lib sempre teve TRÊS arquiteturas em `src/core/` — #1 módulos-plugin (`Shell`+`Discovery`, Spec 04, o que o `Sarak-MyService` USA), #2 renderizador de páginas por manifesto (`Manifest`, o que FALHOU e ninguém usa), #3 componentes atômicos+Provider+Design Engine — e a decisão do mantenedor foi assumir o #1 como modelo oficial, com o Design Engine como central de layout e os componentes atômicos (#3) como blocos; remover só o #2 e o backend próprio.
>
> **(Histórico original)** **A Sarak-Lib-UI-Core é um renderizador genérico.** Ela declara CONTRATOS (tokens, gramática do manifesto, porta de persistência, fronteira de autenticação) e nunca escolhe a infraestrutura do consumidor (banco, provider, linguagem de backend, sistema de auth). Qualquer sistema que a importe deve montar 100% da sua interface via manifestos JSON — o piso de funcionalidade é o sistema `Sarak-MyService`. Origem deste princípio: dois testes reais de instalação plug-and-play (consumidores descartáveis `automacao` e `ERP`, 2026-07) + 2 relatórios de erro de agente, registrados em `00-progresso.md`.

## Antes de começar a editar (leitura obrigatória)

1. **Skill `ui-contexto-repositorio`** — acione primeiro, sempre. Carrega as regras estruturais/limites do módulo (3 camadas, Zero Hardcode, Zero Any, paridade 1:1:1:1:1:1, Configuração vs. Expansão) e as specs fundacionais de `specs/specs/`.
2. **`specs/plan/00-progresso.md`** — leia antes de tocar em qualquer spec. É o log de quem já executou o quê, como, e o que ficou pendente.
3. **A spec que você vai executar** (`specs/plan/NN-*.md`) — leia inteira, incluindo `relacionados:` no frontmatter.

## Skills de execução (acione conforme o tipo de tarefa que a spec pedir)

- **`ui-arquitetura-design`** — ao criar/revisar tokens e CSS Variables.
- **`ui-novo-componente`** — Expansão (token/componente novo, paridade 1:1:1:1:1:1 — a 6ª camada é o Registry do Manifesto).
- **`ui-refatorar-componente`** — deletar ou mudar assinatura de token/componente existente.
- **`ui-criar-preset` / `ui-criar-tema`** — presets/temas visuais (Configuração, não Expansão).
- **`ui-integra-consumidor`** — quando a spec muda algo que o sistema consumidor precisa acoplar.
- **`ui-auditoria-modulo`** — rode **sempre** antes de declarar uma spec concluída (`node .agents/skills/ui-auditoria-modulo/scripts/run_audit.mjs`, meta: 0 falhas).
- Gates permanentes que nenhuma execução pode deixar vermelhos: `npm run barrel:check` (paridade de barril público), `npm run catalog:check` (component-catalog), `npm run build` (com DTS), `run_audit.mjs` no **baseline**. Suíte de teste: `npx vitest run` (completa). *(Obsoletos com a Spec 46: `RegistryParity` e `src/core/Manifest/` foram removidos — não são mais gate.)*

## Ao terminar de executar uma spec (obrigatório)

1. Atualize o `status:` no frontmatter da spec executada.
2. Marque o checkbox no Roteiro de Execução abaixo.
3. Adicione uma entrada em `specs/plan/00-progresso.md` (formato definido lá).

---

## Roteiro de Execução (ordem única — siga de cima para baixo, um item por vez)

> **Prompts prontos:** `specs/plan/00-prompts-execucao.md` contém um prompt autocontido para cada item **ainda pendente** (hoje **P48, P49, P50**), numerado conforme o item do roteiro abaixo. Os prompts dos itens concluídos foram removidos; o histórico está no git e no `00-progresso.md`.
>
> 🏅 **SELO DA ONDA CONCEDIDO em 2026-07-21** (item 15, 2ª execução da Spec 26 — 9,3/10, 0 FAIL). A Fase 4/5 de teste de instalação está encerrada; o que segue é a Fase 6 (Teste Real + polimento). Evidência consolidada: `RELATORIO-INSTALACAO-CONSOLIDADO.md`.

### Fase 1 — Renderização correta e modos de consumo (destrava o piso plug-and-play)

1. [x] **[16 - Tokens Semânticos e Validação de Valores](./16-tokens-semanticos-e-validacao-de-valores.md)** — BUG: `gap: "spacing-md"` vai cru pro CSS e é descartado em silêncio; catálogo não documenta VALORES permitidos. Resolutor oficial + warn em valor inválido + seção de tokens gerada no catálogo + gate. ✅ **Concluída (2026-07-17).**
2. [x] **[17 - Resiliência Leniente e DX de Erros](./17-resiliencia-leniente-e-dx-de-erros.md)** — erro de AUTORIA (ex.: `actions` como objeto) degrada por diretiva com warn, nunca derruba a tela; telas DX para payload ausente/inválido. ✅ **Concluída (2026-07-17).**
3. [x] **[18 - Shell Consome Design Engine](./18-shell-consome-design-engine.md)** — topbar/sidebar do shell do manifesto consomem os tokens que o Design Engine já emite (`--sarak-topbar-bg/height`, `--sarak-sidebar-bg`); `SarakShellNav.orientation` (`auto` segue `navigationStyle`). Paridade MyService. ✅ **Concluída (2026-07-17)** (E2E browser pendente no harness Puppeteer).
4. [x] **[24 - Modo Embarcado e Adoção Incremental](./24-modo-embarcado-adocao-incremental.md)** — a lib atende 2 modos: App (sistema nasce com a lib — validado) e Embarcado (renderizar via manifesto SOBRE frontend existente — hoje o Provider age como dono da página: preflight global, `document.title`, overlays fixos). `mode: 'app'|'embedded'`, CSS escopado (`.sarak-scope`), gate E2E de não-vazamento bidirecional, pergunta de modo na skill de importação (a pergunta JÁ existe na skill com aviso provisório — esta spec completa o fluxo). ✅ **Concluída (2026-07-18).**

### Fase 2 — Portas de infraestrutura (a lib declara contratos, não escolhe infra)

5. [x] **[19 - Porta de Persistência de UI](./19-porta-de-persistencia-ui.md)** — a lib declara só a estrutura das suas tabelas e operações; `schema`/`tablePrefix` configuráveis (fim do `ui_core` imposto); interface `UIStorageAdapter` (pg/sqlite = implementações de referência; Supabase/Firebase/etc. = exemplos documentados); contrato REST dos 5 endpoints documentado para backend em qualquer linguagem. ✅ **Concluída (2026-07-18).**
6. [x] **[20 - Fronteira de Autenticação](./20-fronteira-de-autenticacao.md)** — a lib só RENDERIZA a tela de login (`SarakAuthScreen` 100% manifestável); token/sessão/provider são do host via interceptors; receita canônica documentada; gate anti-acoplamento. ✅ **Concluída (2026-07-19).**

### Fase 3 — Redução de escopo e camada de instrução/instalação

7. [x] **[23 - Remoção do Design Agent](./23-remocao-design-agent.md)** — remove o agente LLM embarcado (`agent-design-operator/`, ~131 arquivos) e toda a superfície na lib (chat card, hooks, `options.designAgent`, tipos públicos, etapas de skill). **BREAKING CHANGE** (o MyService injeta `designAgent`) e **2 decisões HITL** (destino do código; manter ou não porta futura). Executar ANTES da 21/22 (a 23 e a 22 editam a skill `ui-integra-consumidor` — nesta ordem não há reconciliação). ✅ **Concluída (2026-07-19).**
8. [x] **[21 - Scaffolder Init](./21-scaffolder-init.md)** + **[22 - Skills de Consumo: Golden Path](./22-skills-de-consumo-golden-path.md)** — **executar em CONJUNTO, nesta ordem** (prompt único P8): o `npx @sarak/lib-ui-core init` gera o boilerplate completo (modo App/Embarcado, Vite+Express monolítico / Next / frontend-only, peerDeps gravadas, skills copiadas) e em seguida as 3 skills de consumo passam a orquestrá-lo (Golden Path, anti-workspace, `source` com states, regra dura de tokens, linguagem de portas). Dependem de 16/19/20/24. ✅ **Concluídas (2026-07-19).**

### Fase 4 — Selo da Onda (teste de aceitação em consumidor real)

9. [x] **[25 - Limpeza dos Testes Práticos](./25-limpeza-testes-praticos.md)** — remoção COMPLETA dos artefatos da Sarak-UI do consumidor de teste `Earendel/ERP` (frontend improvisado, adapter manual, skill copiada), preservando 100% do negócio do ERP; 3 decisões HITL (.env / schema remoto / commit). Prompt **P9**. ✅ **Concluída (2026-07-20).**
10. [x] **[26 - Instalação Teste (Selo da Onda)](./26-instalacao-teste.md)** — importar a Sarak-UI no ERP limpo com o objetivo de **TESTAR a instalação, não só instalar**: agente EXTERNO sem contexto, só caminho oficial (`init` + skills + catálogo), **contornos proibidos** (obstáculo se REGISTRA, não se contorna), matriz de medição **M1-M10** com evidências + `RELATORIO-INSTALACAO-UI.md` com veredito 0-10. Selo concedido se M1-M10 = PASS; senão, os achados viram a próxima rodada de correção e o teste se repete. Prompt **P10**. ✅ **Concluída — SELO CONCEDIDO (2026-07-21)** na 2ª execução (item 15): rodada 1 negada (6,5/10) → correções (27/28/29/30) → rodada 2 **9,3/10**, 0 FAIL.

### Fase 5 — Correção pós-Selo (fecha os achados do Selo negado e reabre o teste)

> Origem: `RELATORIO-INSTALACAO-CONSOLIDADO.md (Anexo B)` + triagem da Spec 26 no `00-progresso.md`. Ordem: os dois FAIL/críticos primeiro (27, 28), depois a robustez de instalação (29), depois **desinstalar o ERP** (31) e só então o **re-Selo** (2ª execução da Spec 26). A Spec **30** (fechamento dos achados residuais das rodadas 1 e 2) roda DEPOIS do re-Selo — não bloqueia o Selo. Por fim, a Spec **40 (Teste Real)** é a **2ª parte do teste**: implementar as funcionalidades REAIS do ERP 100% via manifesto, corrigindo qualquer lacuna NA FONTE. **A validação final de cada correção é o re-teste real, não a suíte unitária.**
>
> **Numeração (reorganizada em 2026-07-21):** a família **40+** é reservada ao que está **por executar**, na ordem de execução — **40** (Teste Real) → **41** (Piso de Bundle) → **42** (Generalizar CardGrid). O fechamento de achados, já concluído, voltou ao número **30**. Entradas antigas do `00-progresso.md` chamam a Spec 30 de "Spec 40" e a Spec 41 de "Spec 43" — o log é append-only e foi preservado; o de→para está registrado na entrada de 2026-07-21.
>
> **Ciclo do re-Selo:** desinstalar (31) → reinstalar do zero e medir (26). O ERP hoje tem a instalação COMPLETA da rodada 1 na raiz — sem a limpeza, o teste mediria uma instalação por cima de outra. O relatório da rodada 1 está arquivado em `RELATORIO-INSTALACAO-CONSOLIDADO.md (Anexo A)`.

11. [x] **[27 - Paridade de navigationStyle no Shell](./27-paridade-navigationstyle-shell.md)** — achado 0 (crítico): `navigationStyle: "topbar"` espreme a nav horizontal dentro do `<aside>` de 240px do `ShellRouterNode` (regressão vs. Shell legado; quebraria a migração do MyService). Realocação de região por `navigationStyle` + fonte única de leitura com o `SarakShellNav`. Reclassifica **M7 → FAIL**. Prompt **P11**. 🟡 **Executada (2026-07-20)** — unitário verde; validação definitiva é o re-Selo (item 15).
12. [x] **[28 - Gate de Submit à prova de erro de autoria](./28-gate-submit-validacao.md)** — achados 1 (M6 FAIL) + M9: o gate de validação EXISTE e funciona, mas é burlado em SILÊNCIO (`submit` no lugar errado / sem form-escopo → api_call envia dado vazio, toast de sucesso). Leniência de posicionamento + `console.warn` defensivo + shape de `validation` documentado (skill/catálogo). Prompt **P12**. 🟡 **Executada (2026-07-20)** — gates verdes; validação definitiva é o re-Selo (item 15).
13. [x] **[29 - Robustez da primeira instalação](./29-robustez-instalacao-pacote.md)** — achados 4/3/2/5: `files`/`.npmignore` no `package.json` (parar de publicar o fonte inteiro); `--help` real + guard de TTY no `bin/sarak-ui.mjs`; skill `ui-integra-consumidor` manda garantir `package.json` (npm init -y) antes do `npm install github:` + documenta "salvar novo tema". Prompt **P13**. 🟡 **Executada (2026-07-20)** — gates/smoke verdes; validação definitiva é o re-Selo (item 15).
14. [x] **[31 - Limpeza da Rodada 2 (desinstalar do ERP)](./31-limpeza-rodada2-erp.md)** — pré-requisito do re-Selo: remove a instalação COMPLETA da rodada 1 da **raiz** do ERP (`src/`, `node_modules/`, `dist/`, `index.html`, `vite.config.ts`, `tsconfig*.json`, `database.sqlite`, `package.json`+lock, skills de UI em `.agents` **e** `.claude`). ⚠️ **Não reusou a Spec 25** — o inventário dela apontava para `frontend/`, pasta que não existia mais (a rodada 1 instalou na raiz). 5 decisões HITL resolvidas. ✅ **Concluída (2026-07-20).**
15. [x] **Re-Selo da Onda (2ª execução da [Spec 26](./26-instalacao-teste.md))** — ERP reinstalado do zero após 27+28+29 e a limpeza (31); protocolo repetido (agente externo, contornos proibidos, matriz M1-M10), com ≥1 tela de grid/cards fechando a lacuna de cobertura da rodada 1. Resultado: **9 PASS · 1 PARCIAL · 0 FAIL — 9,3/10** (era 6,5/10). ✅ **SELO DA ONDA CONCEDIDO (2026-07-21)** — decisão do mantenedor; o único PARCIAL (M9, `src/` no pacote) teve a causa eliminada pela Spec 30 e verificada por `npm pack`, sem exigir rodada 3. Evidência: `RELATORIO-INSTALACAO-CONSOLIDADO.md`. Prompt P15 removido (não há mais o que executar).
16. [x] **[30 - Fechamento de Achados pós-Selo](./30-fechamento-achados-pos-selo.md)** — *(nasceu como "Polimento pós-Selo", foi EXPANDIDA e passou temporariamente por 40; voltou a 30 na reorganização de 2026-07-21, já concluída)* — fecha os achados residuais das rodadas 1 e 2 do Selo: chave natural do `renderFor` (`hash`/`key`/`slug` + warn dedup), `manualChunks` no `vite.config.ts` do `init` (medido de verdade — ganho é cache do vendor-react, não redução do chunk principal), warning de `input[type=color]` corrigido, **`src/` zerado do pacote** (M9→PASS puro), **`SarakActionCard` generalizado in-place** (decisão HITL: nenhum consumidor real usava o painel LLM). Achado correlato em `SarakCoreCard`/`SarakCardGrid` (mesmo defeito, maior superfície, tipo público já publicado) extraído para a **[Spec 42](./42-generalizar-cardgrid-corecard.md)**. Prompt **P16**. ✅ **Concluída (2026-07-21)** — gates verdes, `run_audit.mjs` sem regressão.
17. [x] **[39 - Importação e Atualização da Biblioteca](./39-importacao-e-atualizacao.md)** — **pré-requisito do Teste Real.** O ERP ficou preso silenciosamente 4 commits atrás (lock pinado em `7fd0bd1`) porque a dependência é git e a `version` nunca muda (`3.0.0` em 8+ commits) — `npm install` vira no-op. NÃO é bug de empacotamento (`npm pack` está correto), é o fluxo de ATUALIZAÇÃO que não existe. Entrega: comando único de atualização gerado pelo `init`, identidade de build verificável (`BUILD_INFO`), seção "como atualizar" na skill, e o **ERP destravado e verificado** (gate que libera a Spec 40). ⚠️ Registra por escrito que **npm não auto-atualiza dependência git** — "sempre a mais atual" é sob comando; automático exigiria registry+semver (fora de escopo por decisão do mantenedor: módulo ainda em desenvolvimento, sem tags). ✅ **Concluída (2026-07-21)** — mecanismo provado num consumidor descartável E no ERP real (`resolved` `7fd0bd1`→`599341c`, `dist/styles/` presente, `src/` ausente); `dist/BUILD_INFO.json` novo (commit+data no `npm run build`); gates verdes (`RegistryParity`, `catalog:check`, `build`, `package:check`, `run_audit.mjs` sem regressão) + suíte completa (301 arquivos/913 testes).
### Fase 6 — Virada Design System (a tese manifesto-only falhou; pivô para design system React)

> Origem: o Teste Real (1ª versão da Spec 40) bateu em 4 paredes numa tela simples → "100% via manifesto" não sustenta produção. **Correção de premissa (2026-07-22):** a lib tem 3 arquiteturas — **#1 módulos-plugin** (`Shell`+`Discovery`, Spec 04, o que o `Sarak-MyService` USA), **#2 renderizador de páginas por manifesto** (`Manifest`, FALHOU/ninguém usa), **#3 componentes atômicos+Provider+Design Engine**. **Remove só o #2 e o backend próprio**; o **Design Engine é a central de layout que aplica ao sistema inteiro** e os componentes atômicos (#3) são os blocos. O módulo não está em produção — construir certo e remover o não-usado. O **Teste Real é o gate empírico** antes de remover o #2. Ordem: 43 → 44 → 45 → **40 (teste)** → 46 → 41 → 42.
>
> **Refino (auditoria do ERP real, 2026-07-24):** há **dois modos legítimos de consumo**, conforme a arquitetura do consumidor, e ambos partilham o mesmo núcleo (Provider + tokens + Design Engine central): **#1 Shell-host** — o `Sarak-MyService` usa (registra módulos no `SarakShell`); **#3 ui-kit + central** — o **ERP Earendel** usa (monorepo React puro, cada módulo é seu web app, Sarak entra como `packages/ui-kit` SEM `SarakShell` como host; removeu o Sarak por ADR 009 e o reintroduz como kit). A **Spec 40 reescrita testa o #3** (a forma real do ERP). O "modelo oficial único" da correção de 22/07 fica relativizado: não é #1 OU #3, é o **núcleo comum** servindo os dois.

18. [x] **[43 - Modelo módulos-plugin oficial (API React)](./43-design-system-primeiro.md)** — 🔑 **ATO ÂNCORA** (enxuto). Reescreve o princípio para "base com Shell + Design Engine central + módulos-plugin (padrão MyService)"; des-depreciar o #1; API pública do modelo (`SarakUIProvider`/`SarakShell`/`registerSarakModule`/`registerLocalComponent` + componentes atômicos); contrato de tokens público (para módulo/componente próprio do importador). ⚠️ Corrige a premissa falsa de que "o MyService rodava componentes atômicos" — ele roda #1. ✅ **Concluída (2026-07-23)** — barrel público ganhou `SarakActionCard`/`SarakSearchCard`/`SarakTitleCard`/`Layouts/`/`Navigation/` (faltavam inteiros); teste real de montagem `SarakUIProvider`+`SarakShell` tematizado; achado colateral (fora de escopo, ver §5.1 da spec): loop de render infinito em `SarakUIProvider` quando `activeThemeId` é passado sem `customThemes` estável — registrado para a Spec 44. Gates verdes, suíte completa 304/933 sem regressão.
19. [x] **[44 - Design Engine central + temas JSON + sem backend](./44-temas-json-e-persistencia.md)** — o **coração do produto**: a central de layout que **aplica a todas as telas** (troca de tema/template repinta tudo que usa tokens); `customThemes`/`initialTheme`/`activeThemeId`/`onThemeChange` formalizados; **validação/segurança real no load** (schema-based, tipo por token via `MASTER_DESIGN_MAP`); CustomizationPanel **exporta JSON** (fim do "salvar tema no banco"); seleção do usuário em **localStorage**. **REMOVIDO o backend próprio** (`backend/`, `dist/backend-node`, export, 2º tsup, pg/better-sqlite3, `endpoints.branding` servidor). Supersede a Spec 19. ✅ **Concluída (2026-07-24)** — corrigido de raiz o bug de loop infinito `customThemes`/`activeThemeId` achado na Spec 43 (guard em `useDesignSync`, não depende mais de referência estável); achado real: `options.endpoints.branding` removido é **BREAKING CHANGE** para o `Sarak-MyService` (fora deste repo, não corrigido aqui). Gates verdes, suíte completa 293/893 (queda de 303/925 = backend removido, esperada).
20. [x] **[45 - Scaffolder (starter padrão MyService) + skills](./45-scaffolder-react-e-skills.md)** — o `init` gera o **starter padrão** (Provider + Shell + Design Engine + módulo de exemplo registrado), sem backend — "importar o módulo e o frontend nascer no padrão"; reescrita da `ui-integra-consumidor` para o modelo módulos-plugin. ✅ **Concluída (2026-07-24)** — executada ANTES da Spec 44 (a pedido do mantenedor); as 3 stacks antigas (vite-express/next/frontend-only) colapsaram numa única (Vite puro, sem backend); smoke real com Playwright confirmou o módulo de exemplo renderizando tematizado; achados corrigidos: `@types/react`/`@types/react-dom` faltando no starter, landing padrão caindo no Design Engine em vez do módulo de exemplo (`defaultModuleId`). Gates verdes, suíte completa 303/925 (−1 arquivo/−8 testes = `serverTs.test.mjs` deletado, esperado).
21. [x] **[40 - Teste Real](./40-teste-real.md)** — *(REESCRITA v5, 2026-07-24)* — a auditoria real do conector corrigiu a v4: o ERP **removeu o Sarak** (ADR 009) e seu front **JÁ É "apps separados"** — cada `web` é um SPA Vite e o conector é um **lançador por redirect de página** (`window.location.replace`), não um host. O teste **readota condicionalmente** o Sarak como **`packages/ui-kit`** consumido por cada `web`, **sem reescrever o conector e sem mono-SPA**. A central atinge todas as telas por **catálogo de temas compartilhado no `ui-kit`** (código) + **`localStorage` mesma-origem** (seleção; provado no deploy único). Regra de ouro: **mexer no mínimo do importador**; Sarak 1x em `ui-kit`; nada de `SarakShell`. Executa em **fases** (Propostas primeiro, reversível). Prova que **a central altera todas as telas** (R5) e que as 4 paredes caem. **Gate empírico** do #2. Pré-req: 43+44+45 (o ADR do ERP superando o 009 é follow-up do dono, não gate). Prompt **P21**. — ✅ **TESTE REAL APROVADO pelo dono (2026-07-25):** o ciclo 40.x (40.1–40.4) foi concluído e validado; R1–R12 verdes (R12/hambúrguer fechado pela correção P21.3-C — detecção real testada + prova headless). **Libera a Spec 46** (P22 é o próximo item da fila).
21.1 [x] **[40.1 - Correções de Importação (rodada 1)](./40.1-correcoes-importacao.md)** — *(NOVA, 2026-07-24)* — rodada 1 do **ciclo iterativo 40.x**: a 1ª importação prática (Spec 40 v5) passou raso (só `accentColor`) e a validação do dono expôs abas quebrando, topbar/sidebar ausente e fontes não mudando. Corrige nas 2 pontas + amplia o objetivo p/ o que o produto exige: **expor TODA a API pública** (gate de paridade de barril — fim do vazamento a conta-gotas), **cromo apresentacional temável** (topbar/sidebar sem Shell-host), **multi-dispositivo exposto**, **temas completos** (cor+fonte+cromo+raio), **origem única em dev**, **`file:` em vez de `.tgz`**. Fecha o R5 amplo + R10 da Spec 40. Fluxo: executa 40.1 → **atualiza a 40** → o dono valida na prática → passa ou abre 40.2. Prompt **P21.1**. ✅ **Concluída** (parte do Teste Real aprovado).
21.2 [x] **[40.2 - Correções de Importação (rodada 2)](./40.2-correcoes-importacao-r2.md)** — *(NOVA, 2026-07-25)* — rodada 2 do ciclo 40.x: a validação prática do dono após a 40.1 achou 3 gaps estruturais. **Cromo em TODOS os apps** (só o conector tinha `SarakAppChrome` → some ao entrar em Propostas); **ícones no cromo** (nav estruturada com ícone first-class); **componentes densos responsivos** (o `SarakDataTable` transborda no mobile — funcionalidade da lib, não export). Princípio novo do dono: **a lib é renderizador GENÉRICO** (monolito/monolito-modular/microsserviço) → cromo é **por-app**, sem host. Composição decidida: **Opção 1** (cada app renderiza o mesmo cromo; reload entre abas aceito). Inclui a demonstração de liberdade (Propostas em cards). Prompt **P21.2**. 🟡 **Executada (2026-07-25):** LIB L1 (`navItems`+ícone first-class) / L2 (`SarakDataCards` colapso mobile) / L3 (repintura completa do cromo) + ERP E1–E3 (4 apps no mesmo `ErpChrome`, nav+ícones compartilhados, Propostas em cards). Gates verdes (299 arq·942 testes, `run_audit` no baseline, 4 web apps tsc verde, zero-gambiarra vazio). **Falta a prova de browser do dono** (checkbox só fecha com ela; senão abre 40.3).
21.3 [x] **[40.3 - Multidispositivo por padrão](./40.3-multidispositivo-por-padrao.md)** — *(NOVA, 2026-07-25)* — rodada 3 do ciclo 40.x: a validação da 40.2 apontou o gap central — a **adaptação de layout multidispositivo** (celular/tablet/desktop) ainda é **pontual** (só `SarakDataTable`→cards). Esta spec torna a adaptação **abrangente e zero-config**: **cromo** reflui por dispositivo (nav colapsa no celular — hambúrguer é só um exemplo), **primitivas de layout** com default mobile-first + `ResponsiveValue`, **densos que o ERP usa** adaptam, + **contrato de responsividade** documentado. É fix de lib (ERP herda). Escopo DELIMITADO (não boilar o oceano: cromo + primitivas + densos do ERP + contrato; resto registrado). Prompt **P21.3**. 🟠 **Executada PARCIAL / reaberta (2026-07-25):** L2 (primitivas mobile-first)/L3 (`SarakTable`→cards)/L4 (contrato) aprovados; **L1 REPROVADO no browser do dono** — o cromo **não colapsa em hambúrguer** no celular (build fresco): os testes usaram `overrideDevice` e nunca exercitaram a detecção real (`useSarakDevice`/`window.innerWidth`). Correção da DETECÇÃO na própria 40.3 via **P21.3-C** (não abre spec nova). Achado ortogonal do mesmo browser (avisos `token fora do contrato`) → **Spec 40.4**. ✅ **Fechada (2026-07-25):** P21.3-C mostrou que a reprovação foi BUILD STALE, não bug; detecção endurecida (`useSarakDevice` self-contained) + teste do caminho real (`SarakAppChrome.viewport.test.tsx`) + prova headless nos 3 viewports. Parte do Teste Real aprovado.
21.4 [x] **[40.4 - Reconciliação do contrato de tokens da Design Engine](./40.4-reconciliacao-contrato-tokens.md)** — *(2026-07-25; o número 40.4 foi reaproveitado — a "kit de uso" virou a Spec 50)* — rodada 4 do ciclo 40.x (correção): a validação de browser da 40.3 expôs avisos `[Sarak:Design] token "X" fora do contrato — descartado`. Auditoria exaustiva achou **117 violações em 21 tokens** (não só a amostra de 9 do console) em defaults + 18 `GLOBAL_THEMES` + 102 presets de componente — drift de contrato interno da lib. Reconciliado **na fonte** (8 tokens ganharam opção de enum legítima; 12 tiveram o valor do tema corrigido para opção canônica), sem afrouxar `validateDesign`, paridade 1:1:1:1:1:1 intacta (409/409/409). ✅ **Concluída e APROVADA pelo dono (2026-07-25)** — gate anti-regressão permanente (`tokenContractParity.test.ts`) + teste de console limpo nos 18 temas (`shippedThemesConsoleClean.test.ts`); suíte 307/996 (0 regressão); gates verdes; `run_audit` no baseline exato; **confirmação de browser do dono (console limpo) + COMMITADA**.
22. [x] **[46 - Remover o renderizador de páginas (#2)](./46-remover-motor-de-manifesto.md)** — ✅ Executado (2026-07-25): `src/core/Manifest/` removido + exports/catálogo/gates/templates/skills do #2. **#1** (`Shell`/`Discovery`) e **#3** intactos. Registry ansioso saiu (bundle re-medido).
23. [x] **[41 - Piso de Bundle: barris de ícone](./41-piso-de-bundle-barris-de-icone.md)** — ✅ **Concluída (2026-07-25).** Chunk de **boot** do consumidor **3203,6 KB → 1533,6 KB (−52,1%)**, medido em app mínimo. Das 3 dimensões, só a §1.2 se confirmou: `lucide-react` 789,2 → 56,5 KB (−92,8%) ao matar o acesso dinâmico ao barril. **§1.3 REFUTADA** — o `tsup` externaliza `dependencies` sozinho, phosphor/tabler nunca estiveram no `dist/` (custam no bundle do consumidor: ~2,6 KB/nome; o `IconMap` 55→100 nomes custou +179,5 KB, preço da não-regressão). **§1.4 REFUTADA** — `export *` vs imports nomeados dá saída byte a byte idêntica; a causa real era o `SarakChartEngine` exportado EAGER no barril, anulando o `React.lazy` que já existia (echarts+recharts+zrender+lodash ≈ 2,9 MB no boot de todo consumidor). Contrato de nomes de ícone publicado no catálogo + `console.warn` em nome fora dele. Prompt **P23**.
24. [x] **[42 - Generalizar SarakCoreCard / SarakCardGrid](./42-generalizar-cardgrid-corecard.md)** — ✅ **Concluída (2026-07-25).** Painel LLM ("Custo In/Out", "Janela de Contexto" com aritmética, "Tokenizer") virou `mapping.details` (pares `{label,value}` já formatados pelo consumidor); subtítulo default `'Modelo'` e os demais textos de domínio (`Ver Specs`/`Descrição Técnica`/`Input-Output Capacities`/ícone por palavra-chave) viraram dado. `price_in`/`price_out`/`context` fora do tipo, que **passou a ser exportado publicamente** (saiu da allowlist do `barrel:check`). Nota de migração em `docs/migracoes.md` (publicado no pacote). Caracterização rodou verde ANTES do refactor. Zero consumidor real afetado (grep no ERP).
### Fase 7 — Soberania do consumidor (identidade + extensibilidade de layout), pós-remoção do #2

> Origem: validação de browser do dono (2026-07-26). Dois achados de DX do consumidor: a lib vazava a própria marca no `document.title` ("Sarak OS"), e o consumidor não tem como injetar imagens/animações/regiões custom no layout do cromo. **Executar ANTES da Spec 50** (o kit documenta a API do consumidor → precisa refletir o contrato de identidade e os slots; o catálogo do kit é gerado por AST e absorve os slots se 48 rodar antes). *(Achado da nav incompleta em apps de módulo é do ERP — hook compartilhado no `@erp/ui-kit` —, não vira spec de lib.)*

25. [x] **[47 - Soberania de identidade do host](./47-soberania-identidade-host.md)** — *(NOVA, 2026-07-26)* — a lib sobrescreve o `document.title` do host com o próprio brand ("Sarak OS") por causa de um default (`useBrandingManager` `tabName:'Sarak OS'` + `useSarakUIEffects` seta sempre). Princípio do dono: **título/favicon/marca são SEMPRE do importador**. Fix: defaults neutros (fim do 'Sarak OS'), a lib só toca a identidade se o consumidor fornecer (opt-in), uma fonte única para `document.title` (hoje 2 setters), Embarcado sem regressão. Prompt **P47**. 🟢 **Executada (2026-07-26)** — 2º vazamento achado além do reportado (`companyName:'Sarak OS'` → `useSarakUI().systemName` → rótulo de marca no cromo). Título unificado no `useSarakUIEffects` (precedência `tabName > systemName`), setter do `DesignInjector` removido; `docs/identidade-do-host.md` novo. Gates verdes (273 arq/808 testes; `run_audit` no baseline). **Falta a validação de browser do dono (§6).** ⚠️ **Ressalva da revisão:** a 47 fechou a FONTE (defaults→`systemName`) e a identidade da página, mas os **SINKS** hardcoded de marca (`'Sarak Lib …'` em `SarakEmptyState`/`SarakSearch`/chat) ficaram de fora — o `SarakEmptyState` inclusive passou a exibir `'Sarak Lib'` no lugar de `'Sarak OS'`. Fechado na **[Spec 49](./49-erradicar-marca-lib-componentes.md)** (item 27).
26. [ ] **[48 - Slots de extensão do `SarakAppChrome`](./48-slots-extensao-layout-chrome.md)** — *(NOVA, 2026-07-26)* — o consumidor precisa **adicionar imagens/animações/qualquer conteúdo** ao layout; hoje só há `brand.logoUrl`/`topbarActions` + fundo global por tema. Fix: **slots nomeados** (`logo`/`topbarStart|End`/`sidebarHeader|Footer`/`banner`/`footer`/`decoration`), opcionais, tematizáveis, responsivos (refluem no mobile, Spec 40.3), sem breaking change. Documentar os 2 níveis (fundo global por tema + slots por região). **Handoff p/ a Spec 50** (caso de autoria novo + ordem 48→50 p/ o catálogo AST absorver). Prompt **P48**.
27. [ ] **[49 - Erradicar a marca da lib em componentes consumidor-facing](./49-erradicar-marca-lib-componentes.md)** — *(NOVA, 2026-07-26; achado da revisão da 47)* — a 47 fechou a identidade da PÁGINA e a FONTE do vazamento, mas os **SINKS** hardcoded ficaram: `SarakEmptyState` (`'Sarak Lib'`/`'Sarak Lib Core Engine'`), `SarakSearch` (`'Sarak Lib Search Engine'`), `ChatHeader` (`'Sarak Lib Engine'`), `SarakChat` (default `'Sarak AI Chat Lab'`) — componentes que o consumidor embute no produto dele. Mesmo princípio: **a lib nunca estampa a própria marca**. Fix: neutralizar (marca do consumidor/`systemName` ou rótulo genérico, nunca `'Sarak …'`) + **gate zero-marca** com allowlist dos painéis internos do Design Engine. `SidebarNav` já está correto (não mexer). Prompt **P49**.
28. [ ] **[50 - Kit de uso do consumidor (`sarak-ui/`)](./50-kit-de-uso-do-consumidor.md)** — *(renumerada de 40.4 → 50 em 2026-07-25; ÚLTIMA da execução)* — fase de ENABLEMENT: artefato de raiz **`sarak-ui/`** shippado no pacote com tudo que o importador precisa para escrever o front — guia único das **4 topologias** + todos os casos + **skill** (`ui-integra-consumidor` reescrita) + **catálogo vivo (JSON)**. **DINÂMICO** (gerado do código, gate `guide:check`), **genérico** (o ERP é só um importador). Integra `init` (45) + `sarak:update` (39). Aceite: um **módulo novo** construído seguindo só o kit. ⚠️ **Executar por ÚLTIMO** (depois de 46/41/42 **+ 47/48/49**): enablement do consumidor só faz sentido com a lib estruturalmente fechada. Deve **absorver o `docs/migracoes.md`** (Spec 42) e os casos de autoria de **identidade** (47) e **extensibilidade de layout/slots** (48). Prompt **P50**.

---

## Backlog arquivado (NÃO executar sem decisão explícita do mantenedor)

### Sub-plano "Design Agent" (specs 01-07) — CANCELADO pela spec 23 (executada)
As specs `01` a `07` evoluíam o agente LLM de temas, removido da biblioteca pela spec 23 (2026-07-19). **Estado real dos arquivos:** `02-mapeamento-semantico-rag-catalogo.md`, `03-separacao-estrutural-chat-acao.md`, `04-multi-preset-diversificado.md`, `05-ingestao-multimodal-html.md`, `06-pipeline-visao-dois-estagios.md` e `07-agente-llm-design-e-expansao-estrutural.md` **já não existem** neste repositório — foram apagados no commit `b8447cb` (bundle acidental numa spec não relacionada, Spec 17), antes mesmo da 23 ser escrita; não há o que marcar/cancelar neles. Só **`01-auditoria-cobertura-componentes.md`** sobreviveu e teve o frontmatter marcado `⚫ Cancelada (plan/23)` (o `git log` continua sendo a fonte do histórico integral). Nota de numeração: a spec 16 ORIGINAL desse sub-plano foi absorvida pela 02 e o arquivo removido — o número 16 foi reutilizado pela Fase 1 (arquivo novo e independente).

### Backlog secundário (specs 10-15) — despriorizado; reavaliar após as Fases 1-3
- **[10 - Responsividade no Gêmeo Digital](./10-responsividade-gemeo-digital.md)** — Tier A corrigido; falta só Tier B (Container Queries reais).
- **[13 - Revisão e Gestão de Brand](./13-revisao-e-upload-de-brand.md)**
- **[11 - Enriquecimento de Presets Visuais](./11-enriquecimento-presets-visuais.md)**
- **[12 - Expansão e Hospedagem de Mídias de Atmosfera](./12-expansao-midias-atmosfera.md)**
- **[14 - Visibilidade da Aba Design Engine](./14-visibilidade-aba-design-engine.md)** — ⚠️ reavaliar após a 23 e a 18: o mecanismo proposto é do shell LEGADO (Discovery); no motor atual a visibilidade é do manifesto do consumidor (basta não declarar a rota `/design`). Pode ter virado obsoleta.
- **[15 - Revisão e Limpeza de Marcadores TODO](./15-revisao-marcadores-todo.md)** — operacional; a 23 remove boa parte dos TODOs junto com o `agent-design-operator/`.
