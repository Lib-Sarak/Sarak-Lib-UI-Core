/**
 * Barril do resolutor de tokens semânticos (Spec 16).
 */
export {
    SPACING_TOKENS,
    SPACING_TOKEN_NAMES,
    isPassthroughCss,
    isResolvableSpacing,
    resolveToken,
    resetTokenWarnings,
    type ResolveTokenOptions,
} from './resolveToken';
